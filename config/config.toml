[network]
localhost = "127.0.0.1"
rx_port = 9001
tx_port = 9000

# triple quotes around text turn it into a raw string, allowing for more customization
[features]
text = """wakeland's projects
currently in development 🙏 dynamic change"""
local_time = false
spotify = true

[dev]
debug = true
